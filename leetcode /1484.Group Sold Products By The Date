# Write your MySQL query statement below
select  sell_date , count(distinct product) as num_sold, GROUP_CONCAT(distinct product SEPARATOR ',') 
as products
from Activities
group by date(sell_date)
-- order by product asc , num_sold desc;
